# COMMIT-TRACKING: UUID-20240803-120000-ENVU 
# COMMIT-TRACKING: UUID-20240805-120000-IMGL
# Description: Added AVAILABLE_IMAGES for storing list of available container images
# Author: Mr K / GitHub Copilot
#
# File location diagram:
# jetc/                          <- Main project folder
# ├── README.md                  <- Project documentation
# ├── buildx/                    <- Current directory
# │   └── .env                   <- THIS FILE
# └── ...                        <- Other project files

# Docker registry URL (optional, leave empty for Docker Hub)
# Example: DOCKER_REGISTRY=myregistry.example.com
DOCKER_REGISTRY=

# Docker registry username (required)
DOCKER_USERNAME=

# Docker repository prefix (required, e.g., the '001' part)
DOCKER_REPO_PREFIX=

# Default base image
DEFAULT_BASE_IMAGE=

# Available container images (semicolon-separated)
AVAILABLE_IMAGES=

# Last used container settings
DEFAULT_IMAGE_NAME=
DEFAULT_ENABLE_X11=on
DEFAULT_ENABLE_GPU=on
DEFAULT_MOUNT_WORKSPACE=on
DEFAULT_USER_ROOT=on
